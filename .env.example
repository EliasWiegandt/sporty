# sporty-frontend (Cloudflare Worker) example env

# Base URL to reach the backend API
BACKEND_URL=http://127.0.0.1:8000

# API key injected by the Worker as X-API-Key
BACKEND_API_KEY=dev-key-123

# These are Worker secrets. Set via:
#   wrangler secret put BACKEND_URL
#   wrangler secret put BACKEND_API_KEY

# Supabase (frontend client)
# New naming: publishable (client) key
# Public values used by the browser SDK (okay to expose).
# For local dev, you can put them into `.dev.vars` instead.
SUPABASE_URL=https://YOUR-PROJECT.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_...
